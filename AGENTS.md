# Agent Instructions

- Always run Python tooling from this repository's virtual environment.
- Use `/home/cpetersjr/doggo/venv/bin/python` instead of system `python`.
- Use `/home/cpetersjr/doggo/venv/bin/pytest` instead of system `pytest`.
- When running scripts or modules, invoke them via the venv Python executable.
